{
  "private": true,
  "license": "MIT",
  "dependencies": {
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-pose": "^3.3.4"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3",
    "typescript": "^3.0.3"
  },
  "scripts": {
    "build": "parcel build src/index.html --no-minify --no-cache",
    "build-ts": "parcel build src/index.html --no-minify --no-cache --experimental-scope-hoisting"
  }
}
